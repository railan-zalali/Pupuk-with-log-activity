<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Stock Report</title>
    <style>
        body {
            font-family: 'DejaVu Sans', sans-serif;
            margin: 0;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .company-name {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .report-meta {
            margin-bottom: 20px;
            color: #666;
        }

        .summary {
            margin-bottom: 30px;
            border: 1px solid #ddd;
            padding: 15px;
        }

        .summary-item {
            margin-bottom: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            font-size: 12px;
        }

        th,
        td {
            padding: 8px;
            border: 1px solid #ddd;
            text-align: left;
        }

        th {
            background-color: #f8f9fa;
        }

        .low-stock {
            color: #dc2626;
            font-weight: bold;
        }

        .total-row {
            font-weight: bold;
            background-color: #f8f9fa;
        }

        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            text-align: center;
            padding: 10px;
            font-size: 10px;
            color: #666;
        }

        .page-break {
            page-break-after: always;
        }
    </style>
</head>

<body>
    <div class="header">
        <div class="company-name">{{ config('app.name') }}</div>
        <div>Stock Report</div>
        <div class="report-meta">
            Report Generated: {{ $date->format('d/m/Y H:i') }}<br>
            Generated By: {{ auth()->user()->name }}
        </div>
    </div>

    <div class="summary">
        <div class="summary-item">
            <strong>Total Products:</strong> {{ number_format($summary['total_products']) }}
        </div>
        <div class="summary-item">
            <strong>Total Stock Value:</strong> Rp {{ number_format($summary['total_stock_value'], 0, ',', '.') }}
        </div>
        <div class="summary-item">
            <strong>Low Stock Items:</strong> {{ number_format($summary['low_stock_count']) }}
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th>Code</th>
                <th>Product</th>
                <th>Category</th>
                <th>Stock</th>
                <th>Min Stock</th>
                <th>Value</th>
            </tr>
        </thead>
        <tbody>
            @foreach ($products as $product)
                <tr>
                    <td>{{ $product->code }}</td>
                    <td>{{ $product->name }}</td>
                    <td>{{ $product->category->name }}</td>
                    <td class="{{ $product->stock <= $product->min_stock ? 'low-stock' : '' }}">
                        {{ $product->stock }}
                    </td>
                    <td>{{ $product->min_stock }}</td>
                    <td>Rp {{ number_format($product->stock_value, 0, ',', '.') }}</td>
                </tr>
            @endforeach
        </tbody>
        <tfoot>
            <tr class="total-row">
                <td colspan="5" style="text-align: right"><strong>Total Stock Value:</strong></td>
                <td><strong>Rp {{ number_format($summary['total_stock_value'], 0, ',', '.') }}</strong></td>
            </tr>
        </tfoot>
    </table>

    <div class="footer">
        {{ config('app.name') }} - Stock Report - Page {PAGE_NUM}/{PAGE_COUNT}
    </div>
</body>

</html>
